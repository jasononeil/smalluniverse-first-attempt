package smalluniverse;

import buddy.*;
using buddy.Should;

class TestSmallUniverse extends BuddySuite {
	public function new() {
		describe("SmallUniverse", {
			it("should correctly set up the page and context in `new()`", {
			});

			it("should run an action an action= parameter was specified", {
			});

			it("should redirect after an action if the action requested it", {
			});

			it("should render the page as HTML by default", {
			});

			it("should render the page as JSON if application/json was requested", {
			});
		});
	}
}