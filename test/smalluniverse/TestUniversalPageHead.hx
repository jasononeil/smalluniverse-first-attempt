package smalluniverse;

import buddy.*;
using buddy.Should;

class TestUniversalPageHead extends BuddySuite {
	public function new() {
		describe("UniversalPageHead", {
			it("should correctly set up and render the <head> element", {
			});
		});
	}
}