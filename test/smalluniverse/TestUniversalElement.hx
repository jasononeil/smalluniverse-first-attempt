package smalluniverse;

import buddy.*;
using buddy.Should;

class TestUniversalElement extends BuddySuite {
	public function new() {
		describe("UniversalElement", {
			it("should be able to be used consistently on the client or the server", {
			});
		});
	}
}